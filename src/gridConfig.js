const GRID_HEIGHT = 35;
const GRID_WIDTH = 42;
const MAX_ROOMS = 20;
const ROOM_SIZE_RANGE = [3, 7];

export const config = { GRID_HEIGHT, GRID_WIDTH, MAX_ROOMS, ROOM_SIZE_RANGE };

export default config;
